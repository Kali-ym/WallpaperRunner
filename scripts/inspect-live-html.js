const fs = require('fs')
const html = fs.readFileSync('fixtures/xchina/live-page1.html', 'utf8')
const bgs = [...html.matchAll(/background-image:url\('([^']+)'\)/gi)].map((m) => m[1])
const photos = bgs.filter((u) => /\/photos\//i.test(u))
console.log('photo bg count', photos.length)
console.log(photos.slice(0, 5).join('\n'))
const originals = photos.map((u) => u.replace(/_600x0\.webp$/i, '.jpg'))
console.log('sample original', originals[0])
fs.writeFileSync('fixtures/xchina/sample-urls.json', JSON.stringify({ photos, originals: originals.slice(0, 5) }, null, 2))
